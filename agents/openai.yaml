interface:
  display_name: "Figma Export Slices"
  short_description: "Download Figma slices (切图)"
  default_prompt: "Export Figma slices using the bundled script. Prefer auto-discovery (--discover) with optional name regex or page regex; otherwise accept explicit slice IDs and export with the requested scales."
